extends ../layout

block content
  h1 #{t('home.list_book')}
  if book_list.length
    ul
      each book in book_list
        li
          a(href=`/books/${book.id}`) #{book.title}
          |  (#{book.author.name})

  else
    p #{t('home.no_book')}
